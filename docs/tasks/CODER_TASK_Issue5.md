你是 PyMultiWFN Ralph Loop 的 Coder Agent。

**当前任务：Issue 5 - 一致性验证（高优先级）**

**进度回顾**：
- ✅ Issue 2 已完成 92%（剩余 9 个低优先级违规）
- ✅ 严重错误已修复（F821, F601）
- ✅ 所有 240 个测试通过

**本次任务（19:40）**：

### 目标：增强 Issue 5 - 一致性验证

**现状**：
- ✅ 已有 8 个一致性测试通过（test_consistency.py）
- ⚠️ 现有测试是理论验证（密度为正、对称性等）
- ⏳ 缺少与已知参考值的数值比较

**增强方向**：
1. **添加参考值验证**
   - 为 H2、H2O、CH4 等简单分子添加参考值
   - 参考：文献值、Gaussian/ORCA 计算结果
   - 验证：电子密度、布居分析、键级等

2. **添加数值精度测试**
   - 测试数值积分精度
   - 测试正交化稳定性
   - 测试矩阵运算收敛性

3. **添加回归测试**
   - 记录关键计算的预期结果
   - 防止未来修改引入偏差
   - 建立基准数据集

**执行步骤**：
1. 检查现有测试数据文件
2. 为简单分子添加参考值测试
3. 验证计算精度（如布居分析、键级）
4. 添加数值稳定性测试
5. Git commit 每个改进

**示例测试用例**：
```python
def test_h2_bond_order_reference():
    """验证 H2 键级的参考值"""
    # H2 键级应该接近 1.0（单键）
    # 参考：文献值或 Gaussian 计算结果

def test_water_mulliken_charges():
    """验证 H2O 的 Mulliken 电荷"""
    # O 原子应该带负电荷
    # H 原子应该带正电荷
    # 参考：标准量子化学计算

def test_electron_count_conservation():
    """验证电子数守恒"""
    # 密度矩阵的迹应该等于电子数
    # 这是基本物理约束
```

**成功标准**：
- [ ] 添加至少 3 个新的参考值测试
- [ ] 所有测试通过（现有 + 新增）
- [ ] 覆盖关键计算（密度、布居、键级）
- [ ] Git commit

**重要**：
- 使用现有测试数据或创建简单的测试分子
- 参考值应该来自可靠来源（文献、标准软件）
- 测试应该快速执行（< 5 秒）
- 明确记录参考值来源

开始工作吧！
